# Solana Configuration
ANCHOR_PROVIDER_URL=https://api.devnet.solana.com
ANCHOR_WALLET=./devnet-wallet.json

# Mock SKR Token (create this on devnet for testing)
SKR_MINT=<your_mock_skr_mint_address>

# Admin wallet public key (for init_global)
ADMIN_PUBKEY=<your_admin_wallet_pubkey>

# Optional: Helius API for webhooks/relayer
HELIUS_API_KEY=<your_helius_api_key>
